{% doc %}
  Renders either the options for the product, or the variant selectors

  @param {product} product - The product
{% enddoc %}

{% liquid
  if product.options.size > 1
    echo 'ADD THE OPTION SELECTORS!!'
  else
    render 'pdp-variant-selectors', product: product
  endif
%}
