{% render 'sticky-add-to-cart' %}

{% schema %}
{
  "name": "Sticky add to cart",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    }
  ],
  "presets": [
    {
      "name": "Sticky add to cart",
      "category": "Buy box",
      "settings": {
        "product": "{{ closest.product }}"
      }
    }
  ]
}
{% endschema %}
